# rspec -d -f documentation mattwynn.rb
require 'rspec'

describe "Front End Future" do
  context "thick web client" do
    context "cons" do
      it "is harder to test"
      it "difficult to crawl(SEO)"
      it "is more prone to get out of date"
    end

    context "pros" do
      it "REST is awesome"
      it "DRY's up code"
      it "* jQuery promises?"
      it "Speed"
    end
  end
end
